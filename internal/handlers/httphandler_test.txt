package handlers

import (
	"net/http"
	"testing"

	_ "github.com/shopspring/decimal"
)

func TestHandler_HandlerNewUserReg(t *testing.T) {
	type fields struct {
		service Services
	}
	type args struct {
		w http.ResponseWriter
		r *http.Request
	}
	tests := []struct {
		name   string
		fields fields
		args   args
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			hn := Handler{
				service: tt.fields.service,
			}
			hn.HandlerNewUserReg(tt.args.w, tt.args.r)
		})
	}
}
